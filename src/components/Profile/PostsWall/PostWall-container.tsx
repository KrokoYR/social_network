import React from "react";

// State:
import {AppState} from "../../../store";

export type UpdateNewPost = React.SyntheticEvent<{ value: string }>;

const mapStateToProps = (state: AppState) => {
	return {
	
	}
}
